apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-wordpress
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: wordpress

  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80

  type: ClusterIP
